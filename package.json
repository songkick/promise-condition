{
  "name": "promise-condition",
  "version": "0.0.1",
  "description": "Rejects a promise based on a condition",
  "repository": {
    "type": "git",
    "url": "https://github.com/songkick/promise-condition.git"
  },
  "main": "index.js",
  "scripts": {
    "test": "tap test.js --cov --coverage-report=lcov",
    "report-coverage": "codeclimate-test-reporter < coverage/lcov.info"
  },
  "keywords": [
    "promise",
    "fetch",
    "async"
  ],
  "author": "dvemac",
  "license": "MIT",
  "devDependencies": {
    "codeclimate-test-reporter": "^0.1.1",
    "tap": "^1.4.1"
  }
}
